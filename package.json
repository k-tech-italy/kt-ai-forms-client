{
  "name": "kt-ai-forms-client",
  "private": true,
  "version": "0.0.7",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:cdn": "vite build && rm dist/assets/index.html",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "test:ui": "vitest --ui",
    "badges": "vitest run --coverage && generateBadges",
    "badges-esm": "vitest run --coverage && generateBadgesEsm"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.2.4",
    "@vitest/ui": "^3.2.4",
    "jsdom": "^26.1.0",
    "node-coverage-badges": "^1.5.2",
    "vite": "^7.1.9",
    "vitest": "^3.2.4"
  },
  "dependencies": {
    "uuid": "^13.0.0"
  }
}
